export const metadata = {
  title: "Sign In",
  description: "Log in to your PromptNest account to access your saved prompts.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}

export const metadata = {
  title: "Create Account",
  description: "Join the community of top prompt engineers on PromptNest.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}